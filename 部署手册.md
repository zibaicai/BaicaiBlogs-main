# my-blog-admin 部署完整流程
云服务器： 8.153.164.215 ，前端走 Next.js standalone（Node 进程，3000 端口），Nginx 80 端口统一反代前端 + 后端。

## 第一步：本地打包前端
### 1.1 确认环境变量文件
确保 my-blog-admin/.env.production 已存在且指向云服务器：
```PlainText
NEXT_PUBLIC_API_BASE_URL=http://www.zibaicai.cn
``` 
走 Nginx 80 端口同源访问，前后端同域，避免 CORS 麻烦。注意：
1. 必须用完整域名（与页面访问域名一致，含 www），不能用相对路径 '/'——服务端 SSR/SSG 的 fetch 不接受相对 URL，会报 Invalid URL。
2. NEXT_PUBLIC_ 变量是构建时固化进 JS 的，改了 .env.production 必须重新 npm run build 才生效。
API 请求 http://www.zibaicai.cn/api/... 会被 Nginx 转发到后端 8080。

### 1.2 清理缓存 + 打包(PowerShell)
在 Windows 本地 PowerShell 执行：

``` PowerShell
cd e:\react\BaicaiBlogs-main\my-blog-admin

# 清理旧产物（重要，避免 Turbopack 缓存干扰）
Remove-Item -Recurse -Force .next

# 用 Webpack 打包（禁用 Turbopack，避免 next/font 字体 bug）
npm run build

```     
打包成功后看到：
```PlainText
✓ Compiled successfully
Route (app) ...
ƒ (Middleware) ...
○ (Static) ...
``` 

### 1.3 打包产物结构
打包完成后， my-blog-admin/.next/ 目录下关键产物：
```
.next/
├── standalone/          ← 核心产物：可独立运行的 Node 服务器
│   ├── server.js        ← 入口文件（启动命令就是 node server.js）
│   ├── package.json
│   └── node_modules/    ← 只含生产依赖（体积小）
├── static/              ← 静态资源（JS/CSS/字体/图片）
└── ...                  ← 其他构建中间文件（部署不需要）
```

⚠️ standalone 模式有两个坑 ：

1. .next/standalone/ 里 没有 .next/static/ ，需要手动复制过去
2. .next/standalone/ 里 没有 public/ ，需要手动复制过去

###  1.4 整理部署目录(PowerShell)
在本地整理出一个完整可运行的目录：
```PowerShell
cd e:\react\BaicaiBlogs-main\my-blog-admin

# 1. 把 static 复制到 standalone/.next/ 下
Copy-Item -Recurse -Force .next\static .next\standalone\.next\static

# 2. 把 public 复制到 standalone/ 下
Copy-Item -Recurse -Force public .next\standalone\public

# 3. 把 .env.production 复制到 standalone/ 下（运行时读取）
Copy-Item -Force .env.production .next\standalone\.env.production

```
完成后 .next/standalone/ 结构：
```PlainText
standalone/
├── server.js            ← 启动入口
├── package.json
├── .env.production      ← API 地址配置
├── .next/
│   └── static/          ← 静态资源
├── public/              ← 公共静态文件
└── node_modules/        ← 生产依赖
```
### 1.5 打成压缩包 (PowerShell)
```PowerShell
# 进入 standalone 目录打包（这样解压后直接是文件，不带 standalone/ 外壳）
cd .next\standalone
tar -czf ..\..\deploy.tar.gz .
cd ..\..

# 验证包大小
Get-Item deploy.tar.gz | Select-Object Name, @{n='SizeMB';e={[math]::Round($_.Length/1MB,2)}}

```
## 第二步：上传到云服务器

### 2.1 上传前端压缩包
```PowerShell
# 本地执行
scp deploy.tar.gz root@8.153.164.215:/opt/
```
### 2.2 服务器端解压
```Bash
# ssh 登录服务器
ssh root@8.153.164.215

# 创建前端目录并解压
mkdir -p /var/www/zibaicai.com
cd /var/www/zibaicai.com

# 备份旧版本（如果有）
# ⚠️ 必须用 find 移动（含 .next / .env.production 等隐藏文件），
#    不能用 mv ./* old_backup/——通配符不匹配隐藏文件，会把 server.js、
#    node_modules、public 移走却留下残缺的 .next，导致服务起不来或静态资源 500
if [ -d old_backup ]; then rm -rf old_backup; fi
mkdir -p old_backup
find . -maxdepth 1 -mindepth 1 ! -name old_backup -exec mv -f {} old_backup/ \;

# 解压新版本
tar -xzf /opt/deploy.tar.gz -C /var/www/zibaicai.com/

# 清理压缩包
rm /opt/deploy.tar.gz

# 验证结构
ls -la /var/www/zibaicai.com/
# 应该看到：server.js  package.json  .next/  public/  node_modules/  .env.production
# 再确认静态资源目录非空（空目录会导致全部静态请求 500）
ls /var/www/zibaicai.com/.next/static/chunks/ | head -3
```

## 第三步：服务器环境准备（首次部署才需要）
### 3.1 安装 Node.js（Next.js 16 需要 18.18+）
```Bash
# 检查是否已安装
node -v 2>/dev/null || echo "未安装"

# Ubuntu 22.04 默认 node 版本太低，用 NodeSource 装 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
apt-get install -y nodejs

# 验证
node -v   # 应该是 v20.x.x
npm -v
```
### 3.2 安装 Nginx
```Bash
apt-get install -y nginx
nginx -v   # 应该是 nginx/1.18+
```
### 3.3 确认后端 jar 已部署
后端 jar 之前已经传过，确认存在：
```Bash
ls -la /opt/my-blog-backend-1.0.0.jar
# 如果没有，从本地重新上传：
# scp e:/react/BaicaiBlogs-main/my-blog-backend/target/my-blog-backend-1.0.0.jar root@8.153.164.215:/opt/
```
## 第四步：创建 systemd 服务（让前后端开机自启 + 崩溃重启）
### 4.1 后端服务
在服务器上创建文件 /etc/systemd/system/baicai-backend.service ：
```Bash
cat > /etc/systemd/system/baicai-backend.service << 'EOF'
[Unit]
Description=BaicaiBlogs Spring Boot Backend
After=network.target mysql.service

[Service]
Type=simple
WorkingDirectory=/opt
ExecStart=/usr/bin/java -jar /opt/my-blog-backend-1.0.0.jar --spring.profiles.active=prod
Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal
SyslogIdentifier=baicai-backend

[Install]
WantedBy=multi-user.target
EOF
```
### 4.2 前端服务
创建文件 /etc/systemd/system/baicai-frontend.service ：
```Bash
cat > /etc/systemd/system/baicai-frontend.service << 'EOF'
[Unit]
Description=BaicaiBlogs Next.js Frontend
After=network.target baicai-backend.service

[Service]
Type=simple
WorkingDirectory=/var/www/zibaicai.com
Environment=PORT=3000
Environment=HOSTNAME=0.0.0.0
ExecStart=/usr/bin/node server.js
Restart=on-failure
RestartSec=5
StandardOutput=journal
StandardError=journal
SyslogIdentifier=baicai-frontend

[Install]
WantedBy=multi-user.target
EOF
```
### 4.3 启用并启动服务
```Bash
# 重载 systemd 配置
systemctl daemon-reload

# 设置开机自启
systemctl enable baicai-backend
systemctl enable baicai-frontend

# 启动后端（先启动后端）
systemctl start baicai-backend

# 等几秒看后端是否启动成功
sleep 5
systemctl status baicai-backend --no-pager
# 应该看到 active (running)，日志里看到 "Started MyBlogApplication"

# 启动前端
systemctl start baicai-frontend

# 看前端是否启动成功
sleep 3
systemctl status baicai-frontend --no-pager
# 应该看到 active (running)
```
### 4.4 验证服务在监听
```Bash
ss -tlnp | grep -E ':(3000|8080)'
# 应该看到：
# LISTEN  0.0.0.0:3000  ... node server.js
# LISTEN  0.0.0.0:8080  ... java
```
### 4.5 日常日志查看
```Bash
# 后端日志（实时滚动）
journalctl -u baicai-backend -f

# 前端日志（实时滚动）
journalctl -u baicai-frontend -f

# 最近 100 行
journalctl -u baicai-frontend -n 100 --no-pager
```
## 第五步：Nginx 配置
### 5.1 创建站点配置文件
在服务器上创建 /etc/nginx/sites-available/baicai.conf ：
```Bash
cat > /etc/nginx/sites-available/baicai.conf << 'EOF'
# 上游：前端 Next.js 进程
upstream baicai_frontend {
    server 127.0.0.1:3000;
    keepalive 32;
}

# 上游：后端 Spring Boot 进程
upstream baicai_backend {
    server 127.0.0.1:8080;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name 8.153.164.215;

    # 上传限制 10MB（与后端 multipart 配置一致）
    client_max_body_size 10m;

    # ========== 后端 API 反代 ==========
    # 所有 /api/ 开头的请求转发到后端 8080
    location /api/ {
        proxy_pass         http://baicai_backend;
        proxy_http_version 1.1;

        # 转发真实客户端 IP（后端日志/限流会用）
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # 长连接
        proxy_set_header   Connection        "";

        # 超时（上传文件可能慢，给宽裕点）
        proxy_connect_timeout 60s;
        proxy_send_timeout    300s;
        proxy_read_timeout    300s;
    }

    # ========== Next.js 静态资源长缓存 ==========
    # _next/static/ 里是带 hash 的 JS/CSS，可永久缓存
    location /_next/static/ {
        proxy_pass         http://baicai_frontend;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        expires            1y;
        add_header         Cache-Control "public, immutable";
    }

    # ========== 前端页面反代 ==========
    # 其他所有路径转发到前端 3000（Next.js 自己处理 SPA 路由）
    location / {
        proxy_pass         http://baicai_frontend;
        proxy_http_version 1.1;

        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # WebSocket 支持（如果前端用了 socket）
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";

        proxy_connect_timeout 30s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }

    # ========== 安全头 ==========
    add_header X-Frame-Options        "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff"     always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin" always;

    # ========== 日志 ==========
    access_log  /var/log/nginx/baicai.access.log;
    error_log   /var/log/nginx/baicai.error.log  warn;
}
EOF
```
### 5.2 启用站点 + 关闭默认站点
```Bash
# 启用我们的站点（创建软链到 sites-enabled）
ln -sf /etc/nginx/sites-available/baicai.conf /etc/
nginx/sites-enabled/baicai.conf

# 关闭 Ubuntu 默认站点（避免 80 端口冲突）
rm -f /etc/nginx/sites-enabled/default

# 验证配置语法（必须看到 test is successful）
nginx -t
```
预期输出：
```PlainText
nginx: the configuration file /etc/nginx/nginx.conf 
syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test 
is successful
```
### 5.3 重载 Nginx
```Bash
nginx -s reload
# 或
systemctl reload nginx
```
### 5.4 确认 Nginx 在监听 80
```Bash
ss -tlnp | grep ':80'
# 应该看到 nginx 在 0.0.0.0:80 监听
```
## 第六步：阿里云安全组放行端口
登录阿里云控制台 → ECS 实例 → 安全组 → 配置规则 → 入方向，添加：

端口范围 授权对象 说明 80/80 0.0.0.0/0 Nginx HTTP 入口（必须开放） 22/22 你的 IP/32 SSH（建议只对你自己开放）

不要开放 3000 和 8080，它们只在本机由 Nginx 反代访问，对外暴露有安全风险。

## 第七步：验证部署
### 7.1 本地直接访问
```PlainText
http://8.153.164.215
```
应该看到前端首页正常加载。

### 7.2 验证 API 链路
```Bash
# 在本地或服务器执行
curl http://8.153.164.215/api/public/posts
# 应该返回 {"success":true,"data":[...]}
```
### 7.3 验证 Nginx 转发
```Bash
# 看请求是否到了后端
tail -f /var/log/nginx/baicai.access.log
# 然后浏览器访问页面，应该看到 /api/ 的请求日志
```

## 后续迭代更新流程
### 更新前端
```PowerShell
# 本地：重新打包 + 上传
cd e:\react\BaicaiBlogs-main\my-blog-admin
Remove-Item -Recurse -Force .next
$env:TURBOPACK = "0"
npm run build

# 整理产物
Copy-Item -Recurse -Force .next\static .next\standalone\.next\static
Copy-Item -Recurse -Force public .next\standalone\public
Copy-Item -Force .env.production .next\standalone\.env.production

# 打包上传
cd .next\standalone
tar -czf ..\..\deploy.tar.gz .
cd ..\..
scp deploy.tar.gz root@8.153.164.215:/opt/
```
```Bash
# 服务器：解压 + 重启前端
ssh root@8.153.164.215
cd /var/www/zibaicai.com
tar -xzf /opt/deploy.tar.gz -C /var/www/zibaicai.com/
rm /opt/deploy.tar.gz
systemctl restart baicai-frontend
journalctl -u baicai-frontend -f   # 看是否启动成功
```
### 更新后端
```PowerShell
# 本地：重新打包 + 上传
cd e:\react\BaicaiBlogs-main\my-blog-backend
mvn clean package -DskipTests
scp target/my-blog-backend-1.0.0.jar root@8.153.164.215:/opt/
```
```Bash
# 服务器：重启后端
ssh root@8.153.164.215
systemctl restart baicai-backend
journalctl -u baicai-backend -f
```

## 常见问题排查
现象 排查命令 可能原因 访问 80 白屏 curl -I http://8.153.164.215 前端没启动 / Nginx 没重载 502 Bad Gateway systemctl status baicai-frontend 前端进程挂了 API 返回 502 systemctl status baicai-backend 后端进程挂了 API 返回 404 cat /var/log/nginx/baicai.error.log Nginx location 配置错 前端调接口跨域 浏览器 F12 Network .env.production 的 API 地址和访问域名不同源 字体加载失败 F12 Console 用了 Turbopack 打包，改用 --webpack


### 启用并验证nginx
```Bash
# 启用站点
ln -sf /etc/nginx/sites-available/baicai.conf /etc/nginx/sites-enabled/baicai.conf

# 关闭默认站点
rm -f /etc/nginx/sites-enabled/default

# 验证配置
nginx -t
```
看到：
```PlainText
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```
## 重载 Nginx
```Bash
nginx -s reload
# 验证 80 端口监听
ss -tlnp | grep ':80'
```
完成后浏览器访问 http://8.153.164.215 验证。

### 2. 数据库连接问题
```Bash
# 测试 MySQL 是否能连
mysql -uroot -p'Z1yeziluochen@gmail.com' -e 
"SELECT 1"

# 看 blogs 库是否存在
mysql -uroot -p'Z1yeziluochen@gmail.com' -e "SHOW 
DATABASES LIKE 'blogs'"

# 看 posts 表结构
mysql -uroot -p'Z1yeziluochen@gmail.com' blogs -e 
"DESCRIBE posts;"
```
### 3. 端口 8080 被占用
```Bash
ss -tlnp | grep 8080
```
如果被别的进程占了，先 kill 掉。

## 清理并重启后端服务
```Bash
# 1. 停掉 systemd 服务（会清理状态）
systemctl stop baicai-backend

# 2. 如果孤儿进程还在，手动 kill
kill 353607 2>/dev/null
sleep 2

# 3. 确认 8080 端口已释放
ss -tlnp | grep 8080
# 应该没有输出

# 4. 重新启动
systemctl start baicai-backend

# 5. 等 10 秒让 Spring Boot 完全启动
sleep 10

# 6. 查看状态（这次应该是 active running）
systemctl status baicai-backend --no-pager
```
预期看到：

```
Active: active (running)
```
### 创建node软链接
```Bash
# 1. 创建 node 和 npm 的软链接到 /usr/bin
ln -sf /root/.nvm/versions/node/v22.23.2/bin/node /
usr/bin/node
ln -sf /root/.nvm/versions/node/v22.23.2/bin/npm /
usr/bin/npm

# 2. 验证
/usr/bin/node -v
# 应该输出 v22.23.2

# 3. 重启前端服务
systemctl restart baicai-frontend
sleep 3
systemctl status baicai-frontend --no-pager
ss -tlnp | grep 3000
```
预期看到 Active: active (running) 且 3000 端口在监听。

## 验证整个链路